# ─── 凭据 (绝不入库) ───────────────────────────────────────────
.env
.env.*
!.env.example
op.env

# ─── 本地配置 (从 .example 生成) ───────────────────────────────
config/review_service.yaml
config/redact_local.yaml     # 本部署自己的敏感词表 — 属本部署机密, 不入库
config/scene.yaml

# ─── 运行产物 (含成员周报原文与评分, 不入库) ────────────────────
cases/
reports/
writing/
_wiki/
*.log

# ─── Python ───────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/
