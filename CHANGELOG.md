# 更新日志

本文件记录**仓库**的版本。

> ⚠️ 别和**评分框架**的版本混淆：框架是《周报自省式诊断》**v7.22**，两者各走各的。
> 框架版本出现在报告抬头与 `v8-coach/SKILL.md`；仓库版本就是这里的 tag。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [SemVer](https://semver.org/lang/zh-CN/)。

---

## [1.0.0] — 2026-08-03

首个可交付版本。从 boss-vault 判断力工程化平台剥离出的**独立部署单元**，可由学习小组自行部署运维。

### 能做什么

- 群里发一份标题含「**周报**」的飞书云文档 → **免 @ 自动评审**，约 5 分钟回一份六段式自省诊断报告
- 也支持 @ 机器人发 Word / PDF / Markdown
- 标题不含「周报」的文档：群里**静默跳过**，单聊回一张说明卡
- 每周一 20:00 自动出上周汇总

评分框架为《周报自省式诊断》v7.22 —— 5 维共 100 分，5 项反向扣分（各 1–2 分，合计上限 10），等级 A / B+ / B / C / C-。

> 🔴 **红线**：分数与等级**不作任何绩效、考核、排名依据**。这是自省诊断工具；一旦用于考核，所有人会开始优化分数而不是优化周报。

### 包含

- `scenes/study-weekly-reflect/` — 评分框架 + v8 教练评委 doctrine（本仓核心资产）
- `scripts/` — 判断引擎（vendored：52 个文件 hash 锁定，4 个场景层可漂移）
- `config/study_weekly_roster.yaml` — 学习小组成员花名册（已授权携带）
- `ops/systemd/` — 长连接 / worker / 周汇总 timer 三件套
- `docs/deploy.md` — 10 步部署指南
- `tests/` — 160 个单测，含剥离冒烟与机密边界断言

### 机密边界（授权范围）

| | |
|---|---|
| ✅ 带走 | 评分框架、评委 doctrine、学习小组成员数据与真实姓名 |
| ❌ 不带 | 上游锚点真名、上游团队真名、第三方客户名、并购尽调材料 |
| ❌ 不带 | 任何凭据（只有 `.env.example`，全空值） |

这条边界有**可执行版本**：`tests/unit/test_smoke_strip.py::TestConfidentialityBoundary`，CI 每次跑。红了说明有越权内容混进来了 —— 去删内容，不要放宽断言。

### 相对上游的删节

剥离时做了三类处置，全部记在 `UPSTREAM.lock`：

1. **整份不带 4 个文件** —— 3 个分身场景模块（含他方真名与战略口径；本仓 `scene_type: review`，该路由运行时不可达）+ 1 份框架早期草稿（夹带第三方尽调材料；运行时只 LOAD `SKILL.md`，移除零影响）
2. **13 个文件删去**上游写死的他方真名与客户名。出站闸 `redact_check.py` 的**结构性规则原样保留**（精确财务数字 / `lark://` 深链 / case_id），具体敏感词改由 `config/redact_local.yaml`（gitignored）驱动 —— 写进代码就等于谁能读这个仓谁就拿到那份名单
3. **摘除**事件路由里 415 行分身分支

> ⚠️ 从上游同步这些文件会把那些名字**带回来**。`scripts/check_vendored.py --list` 列出删了什么，同步后跑 `make smoke` 复查。

### 已知行为（不是 bug）

- 日志出现 `wiki_query_fallback` / `context.md` 为空 —— 预期。本机器人不接知识库，评委只看被评的那份周报
- 群里发非周报文档完全没反应 —— 预期，见触发规则
- `ss -ltnp` 查不到监听端口 —— 预期。飞书长连接是**出站**的
